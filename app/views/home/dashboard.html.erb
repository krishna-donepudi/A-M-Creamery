<%= render :partial => "partials/title_with_search", locals: { title: "#{current_user.first_name}'s Dashboard "} %>
<% if current_user.role?(:admin) %>
    <%= render partial: 'admin_dashboard' %>
<% else %>
     <%= render partial: 'manager_dashboard' %>
<% end %>